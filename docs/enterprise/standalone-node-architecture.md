# Superseded architecture note

This document is retained only to avoid broken historical links.

The active and mandatory KIDE architecture reference is:

- `docs/architecture/client-compute-reference.md`

All KIDE domain computation now runs in the browser. The Node/TanStack backend is restricted to
authenticated persistence, queries, and infrastructure communication.
